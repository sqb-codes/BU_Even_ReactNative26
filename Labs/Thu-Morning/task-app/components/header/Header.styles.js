import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    header: {
        marginVertical: 10,
        paddingVertical: 10,
        backgroundColor: "#3884FF"
    },
    title: {
        fontSize: 20,
        fontWeight: "bold",
        textAlign: "center",
        color: "#fff"
    }
})