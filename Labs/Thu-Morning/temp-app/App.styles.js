import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "space-evenly"
    },
    title: {
        fontSize: 30,
        color: "white",
        fontWeight: "bold",
        textAlign: "center"
    },
    bg_styles: {
        height: "100%",
        resizeMode: "cover"
    }
});