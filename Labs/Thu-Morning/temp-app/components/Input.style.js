import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputBox: {
        fontWeight: "bold",
        backgroundColor: "white",
        height: 50,
        borderRadius: 10,
        paddingLeft: 15
    },
    inputWrapper: {
        width: "80%",
        margin: "auto"
    }
});